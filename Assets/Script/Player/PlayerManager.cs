using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerManager : MonoBehaviour
{
    public static bool is_player_dead = false;

    public static bool is_using_item = false;

    public static bool is_moving_along_x = false, is_moving_along_z = false;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
